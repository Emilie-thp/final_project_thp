<div class="container-fluid">
  <br>
    <div class="card">
      <div class="card-header">
        <h2 class="section-title mb-40 text-center">Message n° <%= @contact.id %></h2>
        <p align ="center"><%= link_to "Retourner aux messages", dashboard_contacts_path, class:"btn btn-info" %>
      </div>
    
      <div class="card-body">
        <div class="card-text">
          <table class="table table-striped table-sm">
              <li>Message reçu le <%= @contact.created_at.strftime("%d/%m/%Y à  %Hh%M") %> </li> 
              <br>
              <li>Envoyé par :<%= @contact.name %></li>
              <br>    
              <li>Ses coordonnées:<%= @contact.email %></li>
              <br>              
              <li>Particulier ou professionnel<%= @contact.status %></li>
              <br>  
              <li>Message:<%= @contact.content %> </li>                           
              </tr>
            </p>
          </table>
        </div>
      </div>
    </div>
  </div>
 </div>