<div class="container">
  <br>
    <div class="card">
      <div class="card-header">
        <h2 class="section-title mb-40 text-center">Gérer les réalisations</h2>
        <p align ="center"><%= link_to "Créer une nouvelle réalisation", new_dashboard_project_path, class:"btn btn-success" %></p>
      </div>
    
      <div class="card-body">
        <div class="card-text">
          <table class="table table-striped table-sm">
            <tr>
              <th>Titre</th>
              <th>Créée le</th>
              <th>Modifiée le</th>
              <th>Par</th>
              <th>Statut</th>
              <th>Edition</th>
              <th>Suppression</th>
            </tr>
            
            <% @projects.each do |project| %>
            <tr>
              <td><%= project.title %>  </td>
              <td><%= project.created_at.strftime("%d/%m/%Y") %> </td>
              <td><%= project.updated_at.strftime("%d/%m/%Y") %> </td>
              <td>Admin n° <%= project.admin.id %> </td>
                <%if project.published %>
                  <td>En ligne</td>
                <%else%>
                  <td>Hors ligne</td>
                <%end%>
              <td><%= link_to "Editer", new_dashboard_project_path, class:"btn btn-info" %></td>
              <td><%= link_to "Supprimer", new_dashboard_project_path, class:"btn btn-danger" %></td>
            </tr>
            <%end%>      
          </table>
      </div>
    </div>
  </div>
</div>
